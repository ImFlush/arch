# home
h cd ~

# exit
:q exit

# ls
l lsd
ls lsd -1

la lsd -a
lsa lsd -a1

lsl lsd -l
lal lsd -al

# vim
v nvim
vim nvim

# sudo
udo udo
s udo
sudo udo

# arch repos
co git --git-dir=/home/tom/repos/arch/ --work-tree=/home/tom

# nmcli
wifi nmcli device wifi
wific nmcli connection

# wireguard
wgtup sudo wg-quick up /etc/wireguard/tom.dedyn.io.conf
wgtdown sudo wg-quick down /etc/wireguard/tom.dedyn.io.conf

wgup sudo wg-quick up
wgdown sudo wg-quick down

# Git
g git

gcl git clone

gs git status

ga git add
gaa git add --all

gb git branch

gc git commit
gcm git commit -m

gd git diff

gl git log
glo git log --oneline

gm git merge

gch git checkout
gchm git checkout master

gr git reset
grh git reset --hard

gpp git pull && git push
gpl git pull
gps git push

# dwm & st
cdwm cd ~/repos/dwm
cdst cd ~/repos/st

dwm-clean cd ~/repos/dwm && make clean && rm -f config.h && rm -f tags && git reset --hard origin/master
st-clean cd ~/repos/st && make clean && rm -f config.h && rm -f tags && git reset --hard origin/master
