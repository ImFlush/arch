interface=$(nmcli -g TYPE,DEVICE connection | grep -E 'wireguard')

if [ "$interface" != '' ]
	then
		wireguard=$(nmcli -g TYPE,DEVICE connection | grep -E 'wireguard' | cut -b 11-)
		echo -n " $wireguard |"
fi
